# Loaded automatically by Vite in `vite` / `npm run dev`.
# Calls go directly to API Gateway (Network tab shows execute-api, not localhost/api).
# Override locally: create `.env.development.local` (gitignored by Vite convention in many setups) or unset to use proxy below.
VITE_API_BASE_URL=https://mgkys4bnl4.execute-api.us-east-1.amazonaws.com
