service: alert-service

# Serverless 3.x JSON schema may lag new runtimes; AWS still accepts them.
configValidationMode: warn
frameworkVersion: '3.x'

plugins:
  - serverless-esbuild
  - serverless-dotenv-plugin
  - serverless-auto-swagger
  - serverless-offline

provider:
  name: aws
  runtime: nodejs22.x
  region: us-east-1
  stage: ${opt:stage, 'dev'}
  deploymentBucket:
    name: ${self:provider.stage}-mvx-alert-service-bucket
    serverSideEncryption: AES256
  memorySize: 512
  timeout: 30

  environment:
    REGION: ${self:provider.region}
    STAGE: ${self:provider.stage}
    NODE_ENV: ${self:provider.stage}
    SERVICE_NAME: ${self:service}
    # Table must exist before deploy; name from custom.alertDynamoTableName.
    ALERT_TABLE: ${self:custom.alertDynamoTableName}
    LOG_LEVEL: ${self:custom.logLevel.${self:provider.stage}, 'info'}
    AWS_NODEJS_CONNECTION_REUSE_ENABLED: '1'
    ERROR_MESSAGES_CDN_URL: https://d2p9v61861q1ox.cloudfront.net
    # Same base URLs as services/sso-integration `custom.stageUrls`.
    USER_SERVICE_BASE_URL: ${self:custom.stageUrls.${self:provider.stage}.USER_SERVICE_BASE_URL}
    ORGANIZATION_SERVICE_BASE_URL: ${self:custom.stageUrls.${self:provider.stage}.ORGANIZATION_SERVICE_BASE_URL}
    USER_SERVICE_TIMEOUT_MS: '8000'
    ORGANIZATION_SERVICE_TIMEOUT_MS: '8000'

  tags:
    Service: ${self:service}
    Stage: ${self:provider.stage}
    ManagedBy: serverless

  iamRoleStatements:
    - Effect: Allow
      Action:
        - logs:CreateLogGroup
        - logs:CreateLogStream
        - logs:PutLogEvents
      Resource: '*'

    - Effect: Allow
      Action:
        - dynamodb:GetItem
        - dynamodb:PutItem
        - dynamodb:UpdateItem
        - dynamodb:DeleteItem
        - dynamodb:Query
        - dynamodb:BatchGetItem
        - dynamodb:TransactWriteItems
        - dynamodb:DescribeTable
      Resource:
        - !Sub 'arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${self:custom.alertDynamoTableName}'
        - !Sub 'arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${self:custom.alertDynamoTableName}/index/*'

package:
  individually: true
  patterns:
    - '!**/*.test.ts'
    - '!**/*.spec.ts'

custom:
  alertDynamoTableName: alert-service-${self:provider.stage}

  dotenv:
    logging: false

  serverless-offline:
    httpPort: 3000
    noPrependStageInUrl: true

  # Per-stage API bases (aligned with services/sso-integration/serverless.yml custom.stageUrls)
  stageUrls:
    dev:
      USER_SERVICE_BASE_URL: 'https://e5gv0qum80.execute-api.us-east-1.amazonaws.com/dev'
      ORGANIZATION_SERVICE_BASE_URL: 'https://9qe3rgipg3.execute-api.us-east-1.amazonaws.com/dev'
    stg:
      USER_SERVICE_BASE_URL: 'https://z79yqfg5n5.execute-api.us-east-1.amazonaws.com/stg'
      ORGANIZATION_SERVICE_BASE_URL: 'https://sqxxye6yl4.execute-api.us-east-1.amazonaws.com/stg'
    prd:
      USER_SERVICE_BASE_URL: 'https://jtoofsw556.execute-api.us-east-1.amazonaws.com/prd'
      ORGANIZATION_SERVICE_BASE_URL: 'https://3i3mbzftx9.execute-api.us-east-1.amazonaws.com/prd'

  logLevel:
    dev: debug
    stg: info
    prd: warn

  esbuild:
    bundle: true
    minify: false
    sourcemap: true
    target: node22
    platform: node
    format: cjs
    tsconfig: ../../tsconfig.base.json
    external:
      - aws-sdk
    keepNames: true
    concurrency: 8
    zipConcurrency: 8
    plugins: esbuild-plugins.js
    resolveExtensions:
      - '.ts'
      - '.tsx'
      - '.js'
      - '.jsx'
      - '.json'

  autoswagger:
    title: Alert Service API
    description: Patient-centric alerts, org queue, user queue, idempotent create (HTTP APIs only)
    version: 1.0.0
    apiType: http
    basePath: /${self:provider.stage}
    schemes:
      - https
      - http
    host: ${self:custom.apiHost, ''}
    generateSwaggerOnDeploy: true
    swaggerFiles:
      - swagger.json
    useStage: true
    swaggerPath: swagger
    excludeStages:
      - prd

# HTTP + CORS pattern aligned with services/sso-integration (no API Gateway TOKEN authorizer; auth in Lambda if needed).
functions:
  health:
    handler: src/handlers/http/health.main
    events:
      - http:
          path: /health
          method: get
          cors: true

  # --- Remaining HTTP APIs commented for health-only pipeline testing; uncomment when ready ---
  # createAlert:
  #   handler: src/handlers/http/createAlert.main
  #   events:
  #     - http:
  #         path: /alerts
  #         method: post
  #         cors:
  #           origin: '*'
  #           headers:
  #             - Content-Type
  #             - Authorization
  #             - X-Correlation-Id
  #             - X-Request-Id
  #           allowCredentials: false
  #
  # getAlert:
  #   handler: src/handlers/http/getAlert.main
  #   events:
  #     - http:
  #         path: /alerts/{alertId}
  #         method: get
  #         cors:
  #           origin: '*'
  #           headers:
  #             - Content-Type
  #             - Authorization
  #             - X-Correlation-Id
  #             - X-Request-Id
  #           allowCredentials: false
  #
  # listPatientAlerts:
  #   handler: src/handlers/http/listPatientAlerts.main
  #   events:
  #     - http:
  #         path: /patients/{patientId}/alerts
  #         method: get
  #         cors:
  #           origin: '*'
  #           headers:
  #             - Content-Type
  #             - Authorization
  #             - X-Correlation-Id
  #             - X-Request-Id
  #           allowCredentials: false
  #
  # listOrgAlerts:
  #   handler: src/handlers/http/listOrgAlerts.main
  #   events:
  #     - http:
  #         path: /organizations/{organizationId}/alerts
  #         method: get
  #         cors:
  #           origin: '*'
  #           headers:
  #             - Content-Type
  #             - Authorization
  #             - X-Correlation-Id
  #             - X-Request-Id
  #           allowCredentials: false
  #
  # listUserAlerts:
  #   handler: src/handlers/http/listUserAlerts.main
  #   events:
  #     - http:
  #         path: /users/{userId}/alerts
  #         method: get
  #         cors:
  #           origin: '*'
  #           headers:
  #             - Content-Type
  #             - Authorization
  #             - X-Correlation-Id
  #             - X-Request-Id
  #           allowCredentials: false
  #
  # patchAlert:
  #   handler: src/handlers/http/patchAlert.main
  #   events:
  #     - http:
  #         path: /alerts/{alertId}
  #         method: patch
  #         cors:
  #           origin: '*'
  #           headers:
  #             - Content-Type
  #             - Authorization
  #             - X-Correlation-Id
  #             - X-Request-Id
  #           allowCredentials: false
