{
  "name": "identity-service",
  "projectType": "application",
  "sourceRoot": "apps/identity-service/src",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "dependsOn": [
        "^build"
      ],
      "options": {
        "outputPath": "dist/apps/identity-service",
        "main": "apps/identity-service/src/index.ts",
        "tsConfig": "apps/identity-service/tsconfig.app.json"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "options": {
        "jestConfig": "apps/identity-service/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "validate-policy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx tools/identity-service/src/index.ts {projectRoot}/serverless.yml {projectRoot}/serverless.yaml --only-changed"
      }
    }
  },
  "tags": [
    "type:app"
  ]
}
