/** Default page size when the client omits `limit`. */
export const DEFAULT_ORG_LIST_LIMIT = 40;

/** Maximum items per page for organization list (handler + DynamoDB). */
export const MAX_ORG_LIST_LIMIT = 250;
