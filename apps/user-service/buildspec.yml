version: 0.2

env:
  shell: bash
  variables:
    STAGE: 'dev'
    DEPLOYMENT_BUCKET: 'dev-mvx-user-service-bucket'
    STACK_NAME: 'dev-user-service'
    NPM_TOKEN: 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

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - echo "Installing dependencies..."
      - npm install --legacy-peer-deps

  pre_build:
    commands:
      - echo "Making scripts executable..."
      - chmod +x apps/user-service/ci/*.sh

  build:
    commands:
      - echo "Starting build..."
      - apps/user-service/ci/build.sh

  post_build:
    commands:
      - echo "Running deployment verification..."
      - apps/user-service/ci/verify-artifacts.sh

artifacts:
  files:
    - apps/user-service/packaged.yaml
    - apps/user-service/.serverless/**/*
  discard-paths: no 