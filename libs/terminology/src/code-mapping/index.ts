/**
 * Terminology / code mapping — placeholder for internal to FHIR code system mapping.
 * No external terminology server dependency required at runtime.
 */
export function mapCode(
  _internalCode: string,
  _system: string,
  _targetSystem: string
): string | undefined {
  return undefined;
}
