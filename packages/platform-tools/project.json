{
  "name": "platform-tools",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/platform-tools/src",
  "projectType": "library",
  "tags": ["type:lib", "type:publishable"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": [
        {
          "projects": ["observability", "event-platform", "middleware", "fhir"],
          "target": "build"
        }
      ],
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "pnpm exec tsup",
        "cwd": "packages/platform-tools"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    }
  }
}
