# eslint-mavrix

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build eslint-mavrix` to build the library.

## Running unit tests

Run `nx test eslint-mavrix` to execute the unit tests via [Jest](https://jestjs.io).
